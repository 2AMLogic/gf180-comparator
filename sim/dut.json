{
  "_comment": [
    "THE DEVICE-UNDER-TEST BINDING. All four experiments instantiate the",
    "subcircuits this file names; none of them contains a comparator netlist.",
    "Swapping the placeholder for the ratified design is a one-line edit here.",
    "  netlist     path relative to sim/, must satisfy the interface contract",
    "              in sim/dut/README.md (checked at load time by harness/dut.py)",
    "  id          short identifier stamped into every record and deck header",
    "  provenance  placeholder | schematic | extracted -- 'placeholder' makes",
    "              every record it produces carry a banner saying the numbers",
    "              substantiate the harness and not a spec row",
    "  params      operating-point parameters the harness emits as .param, so",
    "              all four benches bias the DUT identically without each",
    "              re-stating the numbers"
  ],
  "netlist": "../design/comparator.spice",
  "id": "comparator-dr0001",
  "provenance": "schematic",
  "description": "DR-0001 topology: a resistively-loaded NMOS differential preamplifier (comparator_dut_analog) feeding a StrongARM latch with isolation inverters and a NOR SR output latch (comparator_dut_latch). See spec/decision-records/DR-0001-comparator-topology.md for the sizing rationale and design/README.md for the xschem source and netlisting command.",
  "params": {
    "dut_ib": 1e-05,
    "dut_vcm": 1.65
  },
  "notes": [
    "dut_ib is the tail-mirror reference current forced into the `ibias` pin.",
    "dut_vcm is the input common mode the front end is specified at. It is a property of the DUT, not of any one testbench, so it lives here.",
    "1.65 V is mid-rail (VDD/2 at the nominal 3.3 V supply) -- there is no CDAC or other driving circuit to inherit a common mode from here, so mid-rail is the natural standalone choice; DR-0001 states this explicitly."
  ]
}
